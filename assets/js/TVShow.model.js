class TVShow {
    constructor (tvShow) {
        console.log(`L'objet série TV a été instancié.`)
        console.log(`Le titre de la série demandée est : ${tvShow.name}.`)
        console.log(`${this.constructor.name} est le nom de ma classe`)
    }
}